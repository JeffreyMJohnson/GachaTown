using UnityEngine;
//using Facebook.Unity;
using System.Collections;

public class SocialHandler : MonoBehaviour
{

    //public
    //InitDelegate isInit;
	// Use this for initialization
	void Start ()
    {
	    //if(!FB.IsInitialized)
     //   {
            
     //       FB.Init(isInit, new HideUnityDelegate());
     //   }
	}
	
	// Update is called once per frame
	void Update ()
    {
	
	}
}
